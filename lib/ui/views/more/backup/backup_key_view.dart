import 'package:flutter/material.dart';

class BackupKeyView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        elevation: 0.0,
        title: Text('Back up'),
      ),
      body: Container(),
    );
  }
}
